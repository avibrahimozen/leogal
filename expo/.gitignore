# Native projects are generated by `expo prebuild` / `expo run:ios` (CNG).
# Don't commit them — they regenerate from app.json + config plugins.
/ios
/android

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Metro
.metro-health-check*

# dependencies
node_modules/

# env (keep .env.example)
.env
.env.*.local

# misc
*.log
.DS_Store
